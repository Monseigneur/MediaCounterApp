<manifest xmlns:android="http://schemas.android.com/apk/res/android"

          package="com.example.MediaCounterApp"
>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application android:allowBackup="true"
                 android:label="@string/app_name"
                 android:icon="@mipmap/ic_launcher"

                 android:supportsRtl="true"
                 android:theme="@style/AppTheme">
        <activity android:name=".Activity.MediaCounterActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Activity.MediaCounterAddActivity"
                  android:parentActivityName=".Activity.MediaCounterActivity"
                  android:windowSoftInputMode="stateVisible"/>
        <activity android:name=".Activity.MediaInfoActivity"
                  android:parentActivityName=".Activity.MediaCounterActivity"/>
        <activity android:name=".Activity.MediaStatsActivity"
                  android:parentActivityName=".Activity.MediaCounterActivity"/>
    </application>

</manifest>
